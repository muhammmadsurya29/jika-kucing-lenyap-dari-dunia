# Workspace Rules

- Automatically commit and push finalized changes to the git repository (origin main) without waiting for explicit user permission. Always ensure the code is in a working state before pushing.
